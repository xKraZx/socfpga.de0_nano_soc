// Generated by PeakRDL-regblock - A free and open-source SystemVerilog generator
//  https://github.com/SystemRDL/PeakRDL-regblock

module benchmark_regs (
        input wire clk,
        input wire arst_n,

        input wire avalon_read,
        input wire avalon_write,
        output logic avalon_waitrequest,
        input wire [9:0] avalon_address,
        input wire [31:0] avalon_writedata,
        input wire [3:0] avalon_byteenable,
        output logic avalon_readdatavalid,
        output logic avalon_writeresponsevalid,
        output logic [31:0] avalon_readdata,
        output logic [1:0] avalon_response,

        output benchmark_regs_pkg::benchmark_regs__out_t hwif_out
    );

    //--------------------------------------------------------------------------
    // CPU Bus interface logic
    //--------------------------------------------------------------------------
    logic cpuif_req;
    logic cpuif_req_is_wr;
    logic [11:0] cpuif_addr;
    logic [31:0] cpuif_wr_data;
    logic [31:0] cpuif_wr_biten;
    logic cpuif_req_stall_wr;
    logic cpuif_req_stall_rd;

    logic cpuif_rd_ack;
    logic cpuif_rd_err;
    logic [31:0] cpuif_rd_data;

    logic cpuif_wr_ack;
    logic cpuif_wr_err;

    // Request
    always_comb begin
        cpuif_req = avalon_read | avalon_write;
        cpuif_req_is_wr = avalon_write;
        cpuif_addr = {avalon_address, 2'b0};
        cpuif_wr_data = avalon_writedata;
        for(int i=0; i<4; i++) begin
            cpuif_wr_biten[i*8 +: 8] = {8{avalon_byteenable[i]}};
        end
        avalon_waitrequest = (cpuif_req_stall_rd & avalon_read) | (cpuif_req_stall_wr & avalon_write);
    end

    // Response
    always_comb begin
        avalon_readdatavalid = cpuif_rd_ack;
        avalon_writeresponsevalid = cpuif_wr_ack;
        avalon_readdata = cpuif_rd_data;
        if(cpuif_rd_err || cpuif_wr_err) begin
            // SLVERR
            avalon_response = 2'b10;
        end else begin
            // OK
            avalon_response = 2'b00;
        end
    end

    logic cpuif_req_masked;

    // Read & write latencies are balanced. Stalls not required
    assign cpuif_req_stall_rd = '0;
    assign cpuif_req_stall_wr = '0;
    assign cpuif_req_masked = cpuif_req
                            & !(!cpuif_req_is_wr & cpuif_req_stall_rd)
                            & !(cpuif_req_is_wr & cpuif_req_stall_wr);

    //--------------------------------------------------------------------------
    // Address Decode
    //--------------------------------------------------------------------------
    typedef struct {
        logic REGS_ARR[256];
    } decoded_reg_strb_t;
    decoded_reg_strb_t decoded_reg_strb;
    logic decoded_err;
    logic decoded_req;
    logic decoded_req_is_wr;
    logic [31:0] decoded_wr_data;
    logic [31:0] decoded_wr_biten;

    always_comb begin
        automatic logic is_valid_addr;
        automatic logic is_invalid_rw;
        is_valid_addr = '1; // No error checking on valid address access
        is_invalid_rw = '0;
        for(int i0=0; i0<256; i0++) begin
            decoded_reg_strb.REGS_ARR[i0] = cpuif_req_masked & (cpuif_addr == 12'h0 + (12)'(i0) * 12'h4);
        end
        decoded_err = (~is_valid_addr | is_invalid_rw) & decoded_req;
    end

    // Pass down signals to next stage
    assign decoded_req = cpuif_req_masked;
    assign decoded_req_is_wr = cpuif_req_is_wr;
    assign decoded_wr_data = cpuif_wr_data;
    assign decoded_wr_biten = cpuif_wr_biten;

    //--------------------------------------------------------------------------
    // Field logic
    //--------------------------------------------------------------------------
    typedef struct {
        struct {
            struct {
                logic [31:0] next;
                logic load_next;
            } val;
        } REGS_ARR[256];
    } field_combo_t;
    field_combo_t field_combo;

    typedef struct {
        struct {
            struct {
                logic [31:0] value;
            } val;
        } REGS_ARR[256];
    } field_storage_t;
    field_storage_t field_storage;
    genvar i0;
    generate
    for(i0=0; i0<256; i0++) begin : gen_regs_arr
        // Field: benchmark_regs.REGS_ARR[].val
        always_comb begin
            automatic logic [31:0] next_c;
            automatic logic load_next_c;
            next_c = field_storage.REGS_ARR[i0].val.value;
            load_next_c = '0;
            if(decoded_reg_strb.REGS_ARR[i0] && decoded_req_is_wr) begin // SW write
                next_c = (field_storage.REGS_ARR[i0].val.value & ~decoded_wr_biten[31:0]) | (decoded_wr_data[31:0] & decoded_wr_biten[31:0]);
                load_next_c = '1;
            end
            field_combo.REGS_ARR[i0].val.next = next_c;
            field_combo.REGS_ARR[i0].val.load_next = load_next_c;
        end
        always_ff @(posedge clk or negedge arst_n) begin
            if(~arst_n) begin
                field_storage.REGS_ARR[i0].val.value <= 32'h0;
            end else begin
                if(field_combo.REGS_ARR[i0].val.load_next) begin
                    field_storage.REGS_ARR[i0].val.value <= field_combo.REGS_ARR[i0].val.next;
                end
            end
        end
        assign hwif_out.REGS_ARR[i0].val.value = field_storage.REGS_ARR[i0].val.value;
    end
    endgenerate

    //--------------------------------------------------------------------------
    // Write response
    //--------------------------------------------------------------------------
    assign cpuif_wr_ack = decoded_req & decoded_req_is_wr;
    // Writes are always granted with no error response
    assign cpuif_wr_err = '0;

    //--------------------------------------------------------------------------
    // Readback
    //--------------------------------------------------------------------------

    logic readback_err;
    logic readback_done;
    logic [31:0] readback_data;

    // Assign readback values to a flattened array
    logic [31:0] readback_array[256];
    generate
    for(i0=0; i0<256; i0++) begin : gen_readback_array
        assign readback_array[i0 * 1 + 0][31:0] = (decoded_reg_strb.REGS_ARR[i0] && !decoded_req_is_wr) ? field_storage.REGS_ARR[i0].val.value : '0;
    end
    endgenerate

    // Reduce the array
    always_comb begin
        automatic logic [31:0] readback_data_var;
        readback_done = decoded_req & ~decoded_req_is_wr;
        readback_err = '0;
        readback_data_var = '0;
        for(int i=0; i<256; i++) readback_data_var |= readback_array[i];
        readback_data = readback_data_var;
    end

    assign cpuif_rd_ack = readback_done;
    assign cpuif_rd_data = readback_data;
    assign cpuif_rd_err = readback_err;
endmodule
